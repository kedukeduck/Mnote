# Android 分享卡片 · Design QA

日期：2026-09-23。对象为已有原生 Android 应用，不是新建 Web 原型。按 image-to-code / design-qa 流程进行实际渲染对比。

## 视觉依据与状态

- Source visual truth：`/root/.codex/generated_images/01a0530d-2b94-74b3-82b6-5454b2a4461e/exec-9abb8f6b-cc84-424d-96e2-829a67285e43.png`，836 × 1882，无设备边框。
- Implementation：`app/build/ui-previews/share-card-full.png`，1080 × 1920，由生产 ShareCardRenderer 输出，不是重新生成的设计图。
- 原生预览页：`app/build/ui-previews/share-card-full-preview.png`，390 × 844，mdpi / density 1；小屏：`share-card-small-preview.png`，320 × 568，独立的对应尺寸配置。
- 二维码落地页：`app/build/ui-previews/share-card-web.png`，Headless Chromium 实际渲染，390 × 844，临时独立账号的合成内容，不访问用户笔记。
- 全模块卡片包含摘录、想法、圈选图、完整图、原文和来源二维码；小屏为纯文字记录加原文二维码，缺失图片选项置灰。

两次将源图与实现图片放入同一个图像检查输入进行全视图比较；第二次同时检查 390 和 320 的原生预览。没有把分开的历史图像浏览冒充拼接对比。按相同内容宽度与归一化比例比较字体、边距和层级，不直接比较不同尺寸的像素坐标。不是 CSS 布局，CSS size 不适用于原生卡片；浏览器落地页为 390 CSS px 宽、density 1。

用户新要求优先于旧稿：缩小引号，改为 9:16 一屏卡片，模块自适应。动态截图使用测试时真实渲染的 Mnote 设置界面及其圈选区域，非稿中的示例山水照片，因此图片题材、横纵比、截图内文字不做像素一致性判断。没有把风景示例图硬编码进产品。

## 发现与迭代

1. [P1，已修复] 系统 serif 回退导致中文摘录变成黑体。内置未修改的 Noto Serif CJK SC Regular 字体和许可证；复查最终图片的中文笔画、衬线及正文的无衬线对比，已恢复目标层级。
2. [P2，已修复] 原生选框动画的截图状态不一致。切换时完成 drawable 状态更新；最终全模块预览中勾选项与卡片内容一致。
3. [P2，已修复] “我的想法”字号和行距过大，挤压完整截图。正文基准从 62 改为 52 px，行距缩紧，恢复思想区细分隔线；最终图中字与图片没有交叠，截图获得更多高度。
4. [P2，已修复] 小屏固定选项区挤压预览。低于 650 dp 高度时选项区改为可滚动的 156 dp；独立小屏测试确认预览高度至少 150 dp，保存按钮可见。点击图片仍可全屏放大。

最终全视图在提供的分辨率下足以直接辨认字体、细线和二维码，无需再裁剪一个重复的局部图；同时检查了更小的原生预览。二维码在最终完整位图上经过实际解码测试，非设计稿里的装饰码。

## 必查表面

- 字体：宋体摘录、无衬线想法、serif Mnote；引号与标签同一标题区，明显小于旧稿。文字自动缩小到可读下限，超限明确报错，不使用省略号或隐藏溢出。
- 空间：统一左右边界、细分隔线、底部二维码区；截图保持比例且不裁剪，页面截图为竖向时自然窄于横向示例。只含少量文字时保留留白，不强行放大字填满画布。
- 色彩：纸色 `#F7F2E8`、正文 `#302E28`、强调 `#A95035`；预览控制区沿用应用已有浅色主题。纸面使用纯色，不生成仿造纹理。
- 图片：来自实际记录文件；可切换批注版；预览和相册写入使用同一个 Bitmap。测试源图片分辨率较低，放大存在原图软化；不把此差异误判为压缩截断。完整长截图一屏缩小后的小字不承诺逐字可读。
- 文案：无标签、时间、记录类型、截图类型标题和“当时的页面”；原文和 URL 不单独排入卡片。截图像素中已有的内容不会自动隐藏，预览区明确提示。二维码的公开范围、确认、取消、失败与撤销均有提示。

## 交互与剩余边界

生产逻辑测试覆盖 63 种模块组合、实时切换、长文和空选择、实际二维码解码、相册成功/失败回滚、确认/取消、账号隔离、记录变更、旋转恢复、保存中重建不重复发布。Android 预览能放大，保存不重排另一张图。

浏览器只渲染静态落地页，没有客户端 JavaScript；截图与 HTTP 请求均成功，未见 Chromium 错误输出。来源按钮 href、原文 HTML 转义、禁止脚本 URL、公开字段范围、撤销后的 404 由真实 HTTP 测试覆盖；未声称已在实体手机扫描或在第三方相册里实测长按识别。

可接受的差异：一屏比例与缩小引号是用户指定；动态截图内容不同；内置字体并非图片中的可识别确切字体；纸张纤维纹理可作为 P3 后续润色。无剩余可操作的 P0/P1/P2 视觉问题。实体 Android 相册厂商兼容性与超大系统字体尚待手动验收。

## 实施核对

- [x] 对照已批准卡片，不另换模板。
- [x] 五项视觉表面与两种手机预览尺寸已检查。
- [x] 实时组合与导出使用同一渲染结果。
- [x] 原文/来源二维码为明确同意后的可撤销快照。
- [x] 剩余差异和真机测试缺口明确记录。

final result: passed

## 2026-09-24 · 1.14.0 自适应排版复查

本节更新前文 1.13 的固定画布和超长内容处理规则，保留原视觉方向，不另换模板。

- 短内容按实际高度生成；略超一屏收紧间距和摘录字号；勾选的想法固定 52 px 且完整保留。其他模块按剩余空间分配，不足时摘录明确标注、页面截图标为局部，圈选图片仍完整缩放。最低需求超过一屏时生成单张长图。
- 实际生产渲染检查：`app/build/ui-previews/share-card-short.png`（1080 × 363）、`share-card-full.png`（1080 × 1920）、`share-card-full-preview.png`（390 × 844）。全模块下操作按钮与预览均可见；页面局部提示与摘录截断提示是独立说明，不伪装成原内容。
- `share-card-context-adjust.png` 为原生调整对话框，滑块在最右端时呈现页面底部；改动即时反馈到卡片，旋转后保留位置，不修改源截图。
- `share-card-long-reader.png` 为实际原生窗口滚动到底部后的截图，可看到“最后一句也必须保留。”和返回预览按钮。不是把超长卡片缩成不可读的整页位图；正文按屏幕宽度绘制并可滚动。
- 预览、放大阅读与长 PNG 共用同一份排版。长图导出按条带绘制，测试验证普通图逐像素一致、长 PNG 的真实高度及底部二维码解码，不用缩略图代替导出。
- 二维码公开范围不变：只发布明确选择的原文和来源。截断摘录不会自动公开其全文或想法，不添加不实的“扫码查看全文”。

复查结果：通过。验证环境仍为 Robolectric 原生图形渲染，未声称实体手机、第三方相册和极长图转发兼容性已验收。超大内容超过安全上限或资源不足时明确失败，绝不截断想法后冒充完整。

## 2026-09-25 · 1.14.1 内容顺序复查

按用户要求，内容顺序改为我的想法、摘录、圈选截图、页面截图；二维码仍为底部独立区域。未选择的模块直接跳过，原有字号、颜色、高度分配与长图规则不变。

重新检查生产渲染 `app/build/ui-previews/share-card-full.png`（1080 × 1920）与原生界面 `share-card-full-preview.png`（390 × 844）：想法完整且位于内容顶部，两张图片连续位于所有文字之后，二维码仍在底部；未见重叠或内容与选择不一致。此为真实原生渲染的合成测试记录，非重新生成的设计图。实体手机及第三方相册的验收边界仍同上。
