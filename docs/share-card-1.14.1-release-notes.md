# Mnote Android 1.14.1-test · 分享卡片顺序

分享卡片内容固定为：**我的想法 → 摘录 → 圈选截图 → 页面截图**。未勾选或没有的模块不占位；二维码继续保留在卡片底部。

预览、放大阅读和相册图片使用同一顺序。完整保留想法、短卡片、一屏适配、其他模块限高及必要时生成长图的规则不变。已保存的分享图片不会被改写；重新生成即可采用新顺序。

Android `com.codex.mnote`，versionCode 30，沿用原测试签名。保存草稿后覆盖安装，不要卸载。可通过设置检查更新或从自有服务器下载安装包；Windows 和后台服务不变。
